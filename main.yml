# Runner script for installing .Net framework 4.6.2

---
tasks:
  
      - name: Install .Net 4.6.2
      clc_blueprint_package:
        server_ids: "{{ item.name }}"
        package_id: 8d5ab1b4-210c-4441-9bab-50351b053aaa
        package_params: {}
      with_items: provisioned.servers
